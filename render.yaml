services:
  - type: static_site
    name: royal-shepherd-frontend
    env: static
    branch: main
    plan: starter
    root: .
    publishPath: .
    buildCommand: echo "No build required"

  - type: web_service
    name: royal-shepherd-backend
    env: python
    branch: main
    plan: starter
    root: backend
    buildCommand: pip install -r requirements/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
